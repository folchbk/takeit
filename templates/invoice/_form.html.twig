<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title">Invoice {{ invoice.id|default('') }}</h3>
    </div>
    <!-- /.box-header -->
    <!-- form start -->
    {{ form_start(form) }}
    <div class="box-body">
        <div class="form-group">
            <label>Invoice client</label>
            {{ form_widget(form.client, {'attr': {
                'class': 'form-control'
            }}) }}
            <div class="form-error">
                {{ form_errors(form.client) }}
            </div>
        </div>
        <div class="form-group">
            <label>CreatedAt</label>
            {{ form_widget(form.createdAt, {'attr': {
                'class': 'form-control item'
            }}) }}
            <div class="form-error">
                {{ form_errors(form.createdAt) }}
            </div>
        </div>
        <div class="form-group">
            <label>Payed</label>
            {{ form_widget(form.updatedAt, {'attr': {
                'class': 'form-control'
            }}) }}
            <div class="form-error">
                {{ form_errors(form.updatedAt) }}
            </div>
        </div>


        {#<div class="form-group" style="background: #FDEBD0; padding: 15px;">#}
            {#<div class="row js-invoice-ingredient-wrapper"#}
                 {#data-prototype="{{ form_widget(form.invoiceIngredients.vars.prototype)|e('html_attr') }}"#}
                 {#data-index="{{ form.invoiceIngredients|length }}">#}
            {#</div>#}

            {#<div class='js-invoice-ingredient-wrapper'><a href='#' class='js-invoice-ingredient-add' style="padding: 25px"><span class='fa fa-plus-circle'></span>Add</a></div>#}
            {#{% for invoiceIngredient in form.invoiceIngredients %}#}
                {#<div class="col-xs-4 js-invoice-ingredient-item form-group" style="padding: 25px">#}
                    {#<a href="#" class="js-remove-ingredient pull-right">#}
                        {#<span class="fa fa-close"></span>#}
                    {#</a>#}
                    {#{{ form_widget(invoiceIngredient.ingredient, {'attr': {#}
                        {#'class': 'form-control'#}
                    {#}}) }}#}
                    {#{{ form_widget(invoiceIngredient.quantity, {'attr': {#}
                        {#'class': 'form-control'#}
                    {#}}) }}#}
                    {#{{ form_errors(invoiceIngredient) }}#}
                {#</div>#}
            {#{% endfor %}#}
        {#</div>#}
    </div>
    <!-- /.box-body -->

    <div class="box-footer">
        <div style="float:right;">
            <a class="btn btn-danger" role="button" href="{{ path('invoice_index') }}">Back to list</a>
            <button type="submit" class="btn btn-success">{{ button_label|default('Save') }}</button>
        </div>
    </div>
    {{ form_end(form) }}
</div>